import React from "react";

import classes from "./Footer.module.css";

const Footer = () => {
  return (
    <footer>
      <p className={classes.country}>United Kingdom</p>
    </footer>
  );
};

export default Footer;
